# Library Index

## Current Collections

| Collection | Focus | Records | Notes |
| --- | --- | ---: | --- |
| `grpo_and_reasoning_rl` | PPO, RLHF, GRPO, RLVR, and variants | 9 | Existing records retain their original evidence boundaries. |

## Entry Rules

Add shared material only through an explicit promotion task. Each record needs
a stable ID, source/version provenance, local artifact paths when applicable,
and an honest evidence boundary. Existing records may use older artifact-status
labels; normalize them only when the underlying evidence is re-audited.

## Known Canonical Evidence

- `library/evidence/arxiv_2503.20783v2_repository_pin.md` pins the
  `understand-r1-zero` repository at a full commit. Its copied raw
  `git ls-remote` capture is stored beside it. Do not infer code correspondence
  or licensing without a fresh, task-local audit.
