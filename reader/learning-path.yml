stages:
  - name: "语言建模与 Transformer 地基"
    route_type: "主干"
    purpose: "先把论文公式恢复成样本、shape、前向、loss 和更新，再建立词元表示、序列条件概率、注意力信息路由和文本生成过程。"
    entry: "从这里开始不要求先会 RNN 或 Transformer；只需要能读向量，并愿意手算一个三词表例子。"
    outcome: "读完应能画出 lookup -> 状态/注意力 -> logits -> softmax -> loss 的完整路径，并说清训练与逐 token 推理为何不是同一时钟。"
    checkpoint: "给定三个 token，能否指出表示从哪里来、哪个状态跨位置传递、softmax 沿哪一轴，以及 loss 的梯度最终更新谁？"
    papers:
      - file: formula-reading-bridge.md
        bridge: "先统一阅读动作：把求和、连乘、softmax、交叉熵、mask、归约和梯度落到一个三词表数值更新，后续不必在每篇论文里重新猜符号。"
      - file: neural-lm-word2vec.md
        bridge: "已经会把公式拆成一次更新后，从词表示与语言模型目标开始，分清词向量、完整概率模型和替代训练目标各自是什么。"
      - file: pretransformer-gpt-lineage.md
        bridge: "静态向量只回答‘一个词查到什么’，还没有回答‘一串词怎样形成可供生成读取的状态’；本页先用 RNN/Seq2Seq 建立状态，再让 Bahdanau attention 按输出位置回看源句，最后接到 GPT 的逐词预训练。"
      - file: contextual-representations-finetuning.md
        bridge: "上一页已经解释 RNN/LSTM 隐藏状态为何随上下文变化；现在再看 ELMo 怎样把不同层的上下文状态交给下游任务，随后用 ULMFiT 与 DAPT/TAPT 分开 frozen feature、全参微调和继续预训练。"
      - file: arxiv-1706.03762.md
        bridge: "上一篇暴露了逐位置循环计算和附加注意力的限制；本篇把注意力升级为整个序列模型的骨架。"
      - file: modern-transformer-block.md
        bridge: "Transformer 骨架已经清楚后，先用论文角色地图区分共同基线、机制论文、现代配方与前沿选读；再沿跨层优化、局部变换、位置表达和历史状态四条路径追踪旧问题、改动对象、不变量与新代价，最后回到 GQA、Llama 3.1、Mistral 和 DeepSeek-V2 的组合关系。"
      - file: decoding.md
        bridge: "上一篇停在 logits 与下一词元概率；本篇固定权重，沿单步截断、序列重评分、grammar/runtime 支持集、多候选聚合和推测执行追踪完整生成系统，并用五层 generation contract 结束阶段 1。"

  - name: "预训练、数据、规模与评测地基"
    route_type: "主干"
    purpose: "沿可见性、任务接口、训练分布和算力预算，理解基础模型能力究竟从哪里来。"
    entry: "进入前应已理解 causal mask、encoder/decoder 和 next-token loss；本阶段先固定 token 与数据人口，再比较目标和规模。"
    outcome: "读完应能把一次模型提升拆成 tokenizer/数据、目标函数、参数与 token 预算、任务协议和评测器，而不是只归因于模型名字或参数量。"
    checkpoint: "看到一个新模型分数时，能否先问清 checkpoint、tokenizer、训练 token、prompt/decode/parser/judge 和比较分母？"
    papers:
      - file: tokenization-data-curation.md
        bridge: "阶段 1 已从 token ID 走到 logits 和解码；本篇回到入口补齐 tokenizer：先区分 tokenizer 统计训练与 causal LM 梯度训练，再亲手走一遍 raw text -> normalization -> BPE/Unigram piece -> ID -> embedding row，并用 GPT/LLaMA/Qwen 理解词表、special token 和 checkpoint ABI。数据筛选、packing 与 mask 只保留为接口边界。"
      - file: arxiv-1810.04805.md
        bridge: "08 已固定 text -> token IDs -> causal decoder -> logits；09 保持 tokenizer/embedding 不变，只改变 Transformer 可见性和 label：先按生成 vs 分类/检索/重排选择 causal decoder 或 bidirectional encoder，再用同一个‘北京’位置理解 BERT 为什么必须扰动答案、ELECTRA 为什么改成全位置来源判断。XLNet/SpanBERT 第二遍读。"
      - file: arxiv-1910.10683.md
        bridge: "09 已把 causal decoder 的生成接口与 bidirectional encoder 的表示接口分开；10 解决二者之间的结构缺口：source 用 encoder 双向读取，target 用 decoder 左到右生成。第一遍只比较 BART 全文重建与 T5 缺失-span短 target；UniLM/UL2 留到第二遍。"
      - file: arxiv-2005.14165.md
        bridge: "09/10 比较了为任务选择 encoder、encoder-decoder、目标与 fine-tuning；11 沿 GPT-1/2/3 追踪任务信息从梯度迁入 context，再用本地可精读的 GPT-4 Technical Report 对照 predictable scaling、RLHF 后行为、能力/安全评测和明确披露边界。"
      - file: scaling-kaplan-chinchilla.md
        bridge: "GPT-3 已展示规模能改变 in-context learning，却没有回答下一次训练怎样分预算；Hestness（2017）和 Sutton（2019）先分别提供跨领域经验 scaling 与 Bitter Lesson 的思想背景，12 再固定 `N/D/C/final loss`，用 Kaplan→Chinchilla 的 matched horizon、IsoFLOP 与经验拟合把‘更大’改写成受约束的训练决策，同时保留部署成本和下游能力尚未进入目标的边界。"
      - file: arxiv-2204.02311.md
        bridge: "12 把一次训练压成 `(N,D,C,loss)`；13 先把 `FFN -> GLU -> SwiGLU -> PaLM` 的前因后果接起来，再展开这个点的内部执行，让一个 batch 经过 2048-token block、540B dense decoder、分片前后向、跨 pod 梯度、z-loss 与故障恢复，分开组件采用、预算最优、系统可行和整模结果。"
      - file: arxiv-2302.13971.md
        bridge: "13 回答超大 dense run 怎样完成；14 改变比较约束，只跟 LLaMA-7B 分开一次性训练账、长期推理账、benchmark 合同与发布工件，判断较小模型多训练何时可能更合算、何时不能外推。"
      - file: evaluation-effective-context.md
        bridge: "14 的 TriviaQA 反例说明‘同等质量’不能只看同一张表；15 冻结训练解释，把业务目标、评测 estimand 与有限样本 estimate 分层，再固定 sample、prompt、decode、parser、judge、aggregation 和有效上下文，为后续 RLHF、Agent 与系统比较提供共同尺子。"

  - name: "训练、并行、稀疏容量与高效适配"
    route_type: "主干"
    purpose: "沿着一条可追责的更新链学习：先解释梯度怎样变成参数，再解释参数/梯度/优化器状态怎样跨 rank 分片，随后定位低精度与归约造成的失真，最后把同一套所有权与证据方法用于 MoE 路由、负载控制和专家扩容。"
    entry: "进入前应能解释 loss 如何产生梯度，以及参数、activation 与 optimizer state 不是同一份内存。"
    outcome: "读完应能把一次训练异常还原为参数、梯度、状态、activation、通信、路由和恢复时钟，并用 matched ablation 区分容量收益、系统收益与纯配方差异。"
    checkpoint: "面对 OOM、吞吐下降、loss spike 或 MoE 质量回退，能否先指出失真的对象、owner、触发时钟、证据指标和不能外推的边界？"
    papers:
      - file: adam-adamw.md
        bridge: "从 SGD→AdaGrad/RMSProp→Adam→AdamW 的历史矛盾开始，固定 `g/m/v/theta` 的更新路径，证明 L2 与 decoupled decay 改的是不同边；再把 optimizer state 作为下一篇要分片的长期对象。"
      - file: megatron-zero.md
        bridge: "沿同一个参数跨两个 rank 走一次 reduce-scatter/all-gather，先区分 GPipe 的深度切分、Megatron 的层内 GEMM 切分与 ZeRO 的状态切分，再用 `16Psi/N` 反推长期账和临时峰值的差别。"
      - file: training-numerical-stability.md
        bridge: "把跨卡更新展开为 `scale→finite-check→unscale→reduce→global-clip→optimizer→checkpoint`，沿 FP16 loss scaling、Norm、Switch router、PaLM spike 到 FP8 tensor-role map 建证据链；稳定性不是一个组件名。"
      - file: moe-switch.md
        bridge: "从 2017 稀疏门控的容量假设和负载反例出发，亲走 8-token Switch top-1：把 router、capacity/drop、residual、expert 梯度和 all-to-all 分开，解释为什么 top-1 是工程折中而非普遍最优。"
      - file: moe-routing-load-balancing.md
        bridge: "在同一 4-token score matrix 上比较 `f_iP_i` 梯度、Expert Choice bucket、loss-free 跨步 bias 和 dropless kernel，固定 membership/compute/通信/质量四个 estimand，再审计 V3 的训练与 serving 分离。"
      - file: moe-expert-upcycling-granularity.md
        bridge: "把前面所有权、数值和路由控制汇合到 checkpoint 生命周期：对 Qwen3-Coder 的 `128/Top-8/768` 设计 `256/Top-16/384` 与 `256/Top-8/768` matched 对照，先验收函数保持、父子分化和系统 tail，再决定 CPT、蒸馏或 SFT 是否值得。"
      - file: mixture-of-depths.md
        bridge: "MoE 已让 token 在同一层选择 FFN；本篇改为选择整块深度，用 4 个 token 区分 MoD 跳过后重入、LayerSkip 后缀终止和 MoR 共享层递归，并核对各自 K/V 与服务时钟。"
      - file: arxiv-2106.09685.md
        bridge: "理解全量训练成本后，固定一个 `2x2` 线性层：冻结 `W0`、只更新 `A/B`，再分开训练、adapter-only 保存与 merged/unmerged 服务三个时钟。"
      - file: peft-adapters-prefix-prompt-qlora.md
        bridge: "LoRA 提供了具体基线；现在保持同一基座、样本和 loss，只改变任务自由度的位置，比较 Adapter、Prefix、Prompt、LoRA 与 QLoRA 谁接收梯度、保存什么、服务时谁仍存在。"
      - file: knowledge-distillation-autoregressive.md
        bridge: "PEFT 改变可训练对象而不改变监督来源；蒸馏继续固定 rollout 人口与梯度接收者，比较完整 logits、离线 response、外部 teacher，以及 OPSD/SDPO 中同源 teacher 多看的参考解或环境反馈怎样改变信号、显存与长程 credit。"

  - name: "指令、偏好与推理后训练"
    route_type: "主干"
    purpose: "先区分提示、监督和推理时计算，再沿偏好数据、策略优化和直接偏好目标进入 reasoning RL。"
    entry: "进入前应理解 causal LM、交叉熵、采样和通用评测合同；PPO 会在本阶段用两动作例重新建立。"
    outcome: "读完应能为 SFT、RM、PPO、DPO、GRPO 分别指出样本来自哪里、scorer 是谁、梯度给谁、哪些模型只在训练时存在。"
    checkpoint: "给定一条 prompt/response，能否画出 generator、reference、reward/verifier、old/current policy 与 loss 的所有权和刷新时钟？"
    papers:
      - file: instruction-cot-self-consistency.md
        bridge: "基础模型训练完成后，用同一道算术题先区分三件事：监督样本怎样更新参数、带步骤提示怎样只改变当前生成、多路径投票怎样只改变最终决策；再进入现代推理后训练。"
      - file: reward-verifier-policy-learning.md
        bridge: "用 `17+26` 的错误抵消轨迹区分结果、过程与可达性；从自动标签、步骤归约和候选覆盖进入搜索，再解释同一个评分器如何服务重排、SFT、策略梯度与 DPO。"
      - file: preference-reward-overoptimization.md
        bridge: "沿用数据库备份任务，手算偏好损失、零点与尺度，再解释策略如何指数放大评分高估；通过 proxy/gold 数值反例与原论文曲线建立独立评估、在线刷新和停止依据。"
      - file: arxiv-1707.06347.md
        bridge: "从两动作策略梯度进入备份、校验、恢复演练三步轨迹，手算 GAE、value target 与 clip；再区分 token/sequence、old/reference、价值估计与因果归因，建立后续 RL 算法的对比基线。"
      - file: neurips-2022-instructgpt.md
        bridge: "PPO、偏好模型和 SFT 三块已经分别清楚；现在固定同一数据库备份问题和两条回答，把 SFT、RM、PPO 的数据生产者、冻结对象、梯度与 checkpoint 时钟连起来。"
      - file: neurips-2023-dpo.md
        bridge: "标准 RLHF 流程明确后，DPO 保持同一 chosen/rejected pair，用 current/reference 四个序列分数直接形成离线 pair loss；显式 RM、critic 和在线 rollout 因而退出这条训练图。"
      - file: process-supervision-test-time-search.md
        bridge: "PPO 与 DPO 已经说明 generator 怎样更新；现在固定 `17+26` 的两条候选并冻结 generator，把 ORM/PRM 训练、best-of-N 选择和后来用 verifier reward 更新 policy 拆成三种动作。"
      - file: reasoning-rl-reductions.md
        bridge: "上一页用 verifier 选择好答案，本页进一步更新 generator：从一次采样与评分进入 RLOO/GRPO，再沿 R1、Dr.GRPO、DAPO、GSPO/SAPO 理解长推理训练的具体问题，最后用 pass@k 区分单次可靠性与解题覆盖。"
      - file: instruct-model-effective-post-training.md
        bridge: "前面已经分别掌握 SFT、DPO、PPO/GRPO 与 verifier；现在聚焦 released Qwen3-Coder MoE，分开任务直接性、full-parameter 披露和公开工件，先测 success/reward density 与 expert/router 健康，再用四种训练信号和 full-parameter vs expert-aware PEFT 的 matched 实验收口。"

  - name: "多模态、工具调用与运行时安全"
    route_type: "主干"
    purpose: "把视觉输入、结构化工具调用、不可信观察和客户端携带的 opaque state 放回同一条 Agent ABI，先理解外部状态怎样连接，再处理权限、重放与攻击边界。"
    entry: "进入前应理解 Transformer hidden/logits、SFT 与基本 observation -> action -> environment loop；不要求先背视觉编码器或安全缩写。"
    outcome: "读完应能分开 pixels/frames、bridge tokens、call tokens、opaque reasoning envelope、schema、permission、executor state 和 utility/security verdict。"
    checkpoint: "面对一条多模态工具轨迹，能否指出视觉 token 在哪里进入 LM、哪个字符串只是候选 action、opaque state 绑定哪个 principal/session、谁拥有授权真值，以及攻击内容怎样被挡在副作用之外？"
    papers:
      - file: multimodal-llm-vision-language-abi.md
        bridge: "先把数据库控制台截图固定为 `420x840`，沿 `pixels -> visual states -> bridge -> logits -> OCR/coordinate/time parser` 走通视觉输入 ABI；先分清 token 数、时间戳和答案解析，再读 Flamingo/LLaVA/Qwen 的完整配方。"
      - file: tool-use-function-calling-abi.md
        bridge: "截图读出的 `snapshot_id=s42` 只是模型上下文；本页将它变成 call tokens，经 parser/schema、permission gate、executor 和数据库终态，接上 ReAct、Toolformer 与 tau-bench 的不同训练/评测对象。"
      - file: agent-runtime-prompt-injection-security.md
        bridge: "工具返回中加入‘恢复未校验 s43’的不可信备注，沿用同一调用 ABI 区分 provenance、semantic content、authorization、side effect 与 verdict，再看层级训练和 stateful injection benchmark 的边界。"
      - file: arxiv-2608.09867.md
        bridge: "上一页的不可信备注至少可见；本篇把它换成客户端不可读、模型可消费的 reasoning envelope，检查跨会话/用户/模型重放，并把 principal、session、predecessor、model 与 purpose 绑定到协议。"

  - name: "Agentic RL：credit、能力边界与系统合同"
    route_type: "主干"
    purpose: "先确定长轨迹 outcome 怎样对齐到 state/step/turn/branch，再用 headroom 与 PASS@(k,T) 检查是否真有能力增量，最后验收环境、harness、token provenance 和 rollout 系统是否保持算法语义。"
    entry: "进入前应掌握 PPO/GRPO 的 advantage、importance ratio 与评测合同，并能画出基本的 observation -> action -> environment loop。"
    outcome: "读完应能判断 reward 对齐到哪个对象、success 是否可信、能力增益属于哪种 estimand，以及异步系统是否仍在优化声称的样本。"
    checkpoint: "一条 Agent 轨迹变成训练 batch 时，能否逐项固定 state、action span、verdict、old log-prob、policy version、route、reduction 和失败责任？"
    papers:
      - file: agentic-rl-credit-assignment.md
        bridge: "PPO/GRPO 的 advantage、ratio 与 reduction 单位已经清楚后，固定 Daily→Weekly 三步轨迹，先走 state→action→transition→verdict→credit→action-token loss；五种 credit 方法放到第二遍。"
      - file: evaluator-judge-validity-cua.md
        bridge: "上一篇暂以审计后的 reward 1 构造 credit；本篇回到同一成功轨迹，解释 raw script 0、VLM 1、human 1 为何会反转训练方向，再把 execution、harness 和 judge error 分账。"
      - file: agentic-rl-capability-failures.md
        bridge: "成功标签的误分边界已经明确后，固定 Atlas 三跳检索 checkpoint C0，用 greedy 失败与 8 条 sampled rollout 手算 PASS@(k,T)，分开 capability、reliability、format 和 harness。"
      - file: agentic-rl-environments-systems.md
        bridge: "先知道要更新什么、怎样证明增量，再把 Daily→Weekly 轨迹变成 13-token 可重放 batch，固定 state、raw verdict、action span、old log-prob、policy version 和系统所有者。"
      - file: async-staleness-rolloutpipe.md
        bridge: "上一页由 v17 生成的同一 batch 排队到 current v19；用 Ctrl+S token 手算 sampled ratio，再分开 policy lag、MoE route mismatch、tail clip 与 complete-group 调度。"

  - name: "可扩展监督与控制评测"
    route_type: "选读分支"
    purpose: "先用通用评测合同固定人口、观察者和统计单位，再区分弱标签训练、人机辅助、辩论、运行时控制与条件行为审计各自能承诺什么。"
    entry: "进入前应理解 reward model、过程监督、PPO，以及评测中的 population、judge 与 aggregation。"
    outcome: "读完应能区分监督、辅助审计、辩论、控制与根因解释，不把局部 judge accuracy 写成高风险安全保证。"
    checkpoint: "面对一个 scalable oversight 结果，能否指出谁知道真值、谁能看证据、失败后是否可停止，以及 utility tax 怎样计算？"
    papers:
      - file: scalable-oversight-control-evaluation.md
        bridge: "Agentic RL 已经暴露环境与 judge 的测量风险；现在固定一段含精确日期后门的 patch，逐一比较弱标签训练、批评、辩论、运行时控制和条件审计能看什么、更新谁、承诺什么。"

  - name: "模型内部审计、知识更新与数据飞轮"
    route_type: "进阶分支"
    purpose: "把模型内部机制、参数知识更新/删除和训练数据人口连成生命周期审计链，区分观察、因果干预、编辑、遗忘与递归数据偏移。"
    entry: "进入前应理解 hidden state、梯度、微调和评测合同；完整线性代数与隐私攻击细节放在第二遍。"
    outcome: "读完应能回答一个行为变化究竟来自哪里、改了哪个对象、是否逼近反事实 reference，以及合成数据如何改变下一代训练分布。"
    checkpoint: "看到‘模型记住了新事实/忘掉了旧数据/合成数据提升了分数’时，能否分别固定 intervention、reference、forget/retain、provenance、长尾与独立评测？"
    papers:
      - file: mechanistic-interpretability-causal-intervention.md
        bridge: "从工具安全页的 `s42/s43` 行为差异出发，先用 clean/corrupt activation patching 测量 `logit(Paris)-logit(Rome)`，再区分 probe、causal trace、ROME 和 SAE 的对象与因果边界。"
      - file: knowledge-editing-machine-unlearning.md
        bridge: "定位不等于更新：把‘保留 30 天’改为‘7 天’，再把客户记录从训练中删除，分别走 ROME/MEMIT 的局部写入与 TOFU/MUSE 的 forget/retain/privacy/reference 合同。"
      - file: synthetic-data-feedback-loops.md
        bridge: "编辑/遗忘暴露了训练人口问题；沿用数据库恢复样本，跟踪 seed、producer、filter、real/synthetic mixture、student loss 和下一代 tail coverage，连接 Self-Instruct、phi-1 与 model collapse。"

  - name: "Embedding 模型系统调研与单向量检索"
    route_type: "主干"
    purpose: "从 BERT hidden state 进入可离线索引的句向量，再建立训练 pair、negative、DPR 双编码器与 Recall@K。"
    entry: "进入前建议已读 BERT 与 tokenizer；只需先理解 hidden state、点积和 softmax，本阶段会重新建立 negative 与 Recall@K。"
    outcome: "读完应能把 encoder/readout、训练 pair/negative、query/passage 向量、index 和 Recall@K 分成不同对象。"
    checkpoint: "给定一批 query/passage，能否写出正例、batch 内负例、相似度矩阵、NLL 和 Recall@K 分别怎样计算？"
    papers:
      - file: embedding-models-lineage-selection.md
        bridge: "统一阅读 embedding：从句向量与对比学习，经 E5/BGE/Qwen3 到小模型、多模态、代码、推理式检索和评测；首读第 1–4 章，其余按本篇目录继续。"
      - file: dpr-dense-retrieval.md
        bridge: "通用对比训练路径明确后，把同一组三条 pair 解释为 question/supporting-passage，复算 row-wise NLL，再发布 passage index 并分开 Recall@K、reader EM 与引用支持。"

  - name: "外部知识与事实性"
    route_type: "主干"
    purpose: "围绕 knowledge_factory 的实际任务，从召回进入条件生成，再区分来源存在、主张被支持和模型置信度。"
    entry: "进入前应已理解 DPR/Recall@K 和阶段 2 的通用评测合同。"
    outcome: "读完应能分开 retrieval coverage、生成正确性、引用支持、校准、采样一致性和真实业务效用。"
    checkpoint: "答案带引用时，能否分别检查候选是否召回、引用是否对应原文、原文是否支持主张、答案是否有用？"
    papers:
      - file: rag-external-memory.md
        bridge: "DPR 已召回重置密码的 `z1/z2`；本篇让两篇文档分别生成，手算 Sequence/Token marginal、责任与 query gradient，再把 citation 验收留成独立接口。"
      - file: factuality-calibration-selfcheck.md
        bridge: "沿用 `z1` 的“链接 10 分钟有效”，构造回答写“24 小时”却自报 0.9 且 5 次稳定复述的反例，拆开正确性、引用支持、校准和一致性。"

  - name: "多向量检索与索引实现"
    route_type: "选读分支"
    purpose: "Embedding 模型与领域选型已合并在阶段 9；本分支深入 ColBERT 的多向量交互、压缩和索引成本。"
    entry: "这是检索选读分支；应先完成单向量 embedding、DPR/Recall@K。RAG 主线不依赖读完本阶段。"
    outcome: "读完应能解释 MaxSim、teacher 监督、残差压缩、候选召回和索引成本。"
    checkpoint: "面对一个 embedding leaderboard，能否先核对 pooling、prefix、dimension、normalization、negative、index、metric 和下游任务？"
    papers:
      - file: late-interaction-multivector-retrieval.md
        bridge: "回到重置密码的 `z1/z2`，把 DPR 的一文一向量换成 token 多向量，手算 MaxSim，并分开 ANN candidate、精排、teacher 监督和残差压缩。"

  - name: "代码生成与软件工程 Agent"
    route_type: "选读分支"
    purpose: "从代码检索候选进入补全、可执行程序和真实仓库修复，建立代码 token、测试 reward、repo revision、ACI 和回归终态的连续接口。"
    entry: "进入前应完成阶段 9 系统调研第 8 章的代码 embedding/retrieval，理解 token logits、pass@k 和基本工具 loop；仓库级环境细节按第二遍回查。"
    outcome: "读完应能把 FIM、compiler/test feedback、SWE-bench resolved 和 SWE-agent ACI 分成模型、环境、接口与评测四个对象。"
    checkpoint: "一个 patch 测试通过时，能否说明它使用了哪个 repo revision、环境、测试分母和 ACI，并指出 syntax、example test、hidden regression 与安全质量仍缺什么？"
    papers:
      - file: code-generation-software-engineering-agents.md
        bridge: "代码检索已经定位 `R17/select_snapshot`；本页把候选文件变成 FIM span、编译/测试反馈和多文件 patch，最后用 SWE-bench/SWE-agent 的干净仓库回放验收终态。"

  - name: "长上下文与序列状态"
    route_type: "进阶分支"
    purpose: "从 KV cache 的服务成本出发，区分窗口容量与有效利用，再比较压缩状态和百万级上下文两条路线。"
    entry: "进入前应理解 Transformer Q/K/V、KV cache、服务 SLO 和通用评测；状态空间公式不是起点。"
    outcome: "读完应能分开 raw context capacity、每 query 访问量、固定递归状态、有效检索能力和端到端时延。"
    checkpoint: "看到‘支持 1M context’或‘无 KV cache’时，能否追问训练长度、状态容量、访问路径、检索任务与真实 prefill/decode 成本？"
    papers:
      - file: inference-serving-quantization.md
        bridge: "评测合同建立后，把第 2 段为 10 分钟、第 15 段为 24 小时的同一请求送入服务：先走 prefill/decode、cache 长度和等待时钟，再把调度、kernel、推测解码与量化放回各自对象。"
      - file: long-context-memory-evaluation.md
        bridge: "请求状态与等待成本已经明确后，沿用同一 16 段提示，从 ids/hidden、远端 attention edge、logits 到 parser 推出位置、容量、前向、训练 credit 与行为五个断点。"
      - file: arxiv-2312.00752.md
        bridge: "full attention 的远端边和 KV 增长已经分开；现在让同一 16 段提示逐 token 压进固定 `h:[B,D,N]`，观察输入相关写入能否保留早期 10 分钟并抑制晚到 24 小时。"
      - file: efficient-attention-delta-state.md
        bridge: "Mamba 展示了有限状态怎样按内容写入；现在把 reset-link 的旧 24 小时关联改写为 10 分钟，比较只追加、按 key 覆盖和全局/channel 衰减，并检查错误信息怎样覆盖回去。"
      - file: native-sparse-attention.md
        bridge: "固定状态的覆盖与碰撞已经清楚后，NSA 回到仍保存 raw K/V 的中间路线：让末尾 query 用摘要粗查第 2 段、回原文精读 10 分钟，同时看见 window 中第 15 段的 24 小时干扰。"
      - file: arxiv-2403.05530.md
        bridge: "状态压缩与访问压缩都走完后，把同一 16 段提示交给 Gemini 报告式 evaluator：分别检查单目标、多目标、冲突证据、API error 分母和闭源 checkpoint 边界。"

  - name: "现代模型系列"
    route_type: "综合验收"
    purpose: "复用现代 block、数据、后训练、MoE 与长上下文知识，按 Llama、Qwen、DeepSeek 三个家族做版本差分；DeepSeek 在同一篇中串联 LLM、V2、V3、R1、V3.2、V4 与 V4.1。"
    entry: "这是综合验收阶段；若 modern block、MoE、训练系统、后训练或长上下文仍不稳，先回到对应分支，不必硬读。"
    outcome: "读完应能把品牌代际叙述改写成架构、数据、训练信号、系统和评测合同的逐项差分。"
    checkpoint: "面对 Qwen/DeepSeek 新版本，能否画出保留、替换、组合和未知四类变化，并拒绝把整模增益归因给一个组件？"
    papers:
      - file: llama1-llama2-llama31-lineage.md
        bridge: "训练、后训练和长上下文前置都完成后，固定数据库恢复 A/B/C：用 Llama 2/3.1 区分 RM 选择、RS-SFT、PPO、DPO 的内层梯度与跨轮数据刷新。"
      - file: qwen2-qwen25-qwen3-lineage.md
        bridge: "沿用恢复 A/B/C，把 Llama 的 chosen/rejected 接到 Qwen2 episodic pair、Qwen2.5 fixed pair 与 current-response scalar/group，再分出 Qwen3 多评价器和学生蒸馏。"
      - file: deepseek-v2-v3-r1-lineage.md
        bridge: "用同一个长程修复任务，比较各代怎样存历史、选择证据、激活专家和训练行为；从 MLA 走到 DSA、CSA 与 CSA2，再比较 R1 响应蒸馏和 V4/V4.1 多教师 OPD。"

  - name: 20260911论文补读路线
    route_type: 选读分支
    purpose: 围绕简历中的 Agent RL 后训练主线补齐奖励、信用分配、目标归约、搜索、蒸馏、评测与系统，并扩展翻译、检索与 F5-TTS。
    entry: 可从本专题入口直接开始；按章节回查 PPO、DPO 和张量/公式前置，不要求读完前面所有阶段。
    outcome: 能把项目方法写成样本、目标、梯度、实验和证据边界，并提出一个可验证的改进。
    checkpoint: 能否对自己的一个项目说清信号来源、优化分母、失败样例、独立评价和下一组消融？
    papers:
    - file: 20260911-reading-route.md
      bridge: 先沿项目问题选择论文，再按四周路线逐项验收。
    - file: 20260911-reward-shaping.md
      bridge: 先分清真实目标、参考相似度和潜势差分，再讨论更细的奖励。
    - file: 20260911-credit-assignment.md
      bridge: 奖励定义后，判断当前轮的优势到底来自哪个状态和比较组。
    - file: 20260911-grpo-objectives.md
      bridge: 把局部优势接入 loss 后，手算 token、sequence 和全局分母。
    - file: 20260911-search-training.md
      bridge: 明确优化目标后，再区分搜索路径、训练样本与分支策略梯度。
    - file: 20260911-distillation.md
      bridge: 搜索提供反馈后，解释教师多看到什么、学生在哪些前缀上学习。
    - file: 20260911-tool-evaluation.md
      bridge: 训练链路走通后，用任务终态与跨次可靠性检验是否真有收益。
    - file: 20260911-rl-systems.md
      bridge: 评价合同固定后，检查采样、权重同步和异步更新是否对应同一分布。
    - file: 20260911-translation.md
      bridge: 用十二问连接 NLLB 的剪枝、词表、训练、偏好选择与长链翻译，并逐项验证。
    - file: 20260911-retrieval-sql.md
      bridge: 再分开摘要索引、跨层召回与 SQL 解析约束解决的问题。
    - file: 20260911-f5tts.md
      bridge: 最后沿文本、mel、向量场和波形，解释质量与首包时延的来源。
